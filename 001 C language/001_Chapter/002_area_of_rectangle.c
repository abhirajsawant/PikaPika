// Write a C program to calculate area of a rectangle:
#include<stdio.h>
int main(){
    float length , width;
    printf("length & width of rectangle respectively: ");
    scanf("%f %f",&length,&width);
    printf("Area of Rectangle: %f", length*width);
}